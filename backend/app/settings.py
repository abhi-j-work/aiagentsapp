# In-memory store for runtime settings.
# In a production app, this might be a database or a config file.
runtime_settings = {
    "groq_model": "llama3-70b-8192",
    "chunk_size": 4000,
    "overlap": 200,
}
